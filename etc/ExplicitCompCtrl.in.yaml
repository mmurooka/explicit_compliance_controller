---
# If true, the FSM transitions are managed by an external tool
Managed: false
# If true and the FSM is self-managed, transitions should be triggered
StepByStep: false
# Change idle behaviour, if true the state is kept until transition,
# otherwise the FSM holds the last state until transition
IdleKeepState: false
# Where to look for state libraries
StatesLibraries:
  - "@MC_STATES_DEFAULT_RUNTIME_INSTALL_PREFIX@"
  - "@MC_STATES_RUNTIME_INSTALL_PREFIX@"
# Where to look for state files
StatesFiles:
  - "@MC_STATES_DEFAULT_RUNTIME_INSTALL_PREFIX@/data"
  - "@MC_STATES_RUNTIME_INSTALL_PREFIX@/data"
# If true, state factory will be more verbose
VerboseStateFactory: false
# Additional robots to load
robots:
  ground:
    module: env/ground
# General constraints, always on
# constraints:
# - type: contact
# - type: dynamics
#   damper: [0.1, 0.01, 0.5]
# - type: compoundJoint
# Collision constraint
collisions:
  - type: collision
    useMinimal: true
# Initial set of contacts
# contacts:
#   - r1: jvrc1
#     r2: ground
#     r1Surface: LeftFoot
#     r2Surface: AllGround
#   - r1: jvrc1
#     r2: ground
#     r1Surface: RightFoot
#     r2Surface: AllGround

# Implement some additional text states
states: {}
# Transitions map
transitions:
  - [ExplicitCompCtrl_Initial, GoToCompliant, ExplicitCompCtrl_Compliant, Auto]
  - [ExplicitCompCtrl_Compliant, GoToInitial, ExplicitCompCtrl_Initial, Auto]
# Initial state
init: ExplicitCompCtrl_Initial

Plugins: [ExternalForcesEstimator]

ObserverPipelines:
  - name: Pipeline
    observers:
      - type: Encoder
        config:
          velocity: encoderVelocities

kinova_bota_ds4:
  tool_frame: "FT_sensor_wrench"

panda_default:
  tool_frame: "Arm"
